<% @books.each do |book| %>
  <tr>
    <td><%= book.name %></td>
    <td><%= book.author %></td>
    <td><%= link_to "Edit", edit_book_path(book), remote: true, class: "btn" %></td>
    <td><%= link_to "Delete", book_delete_path(book), remote: true, class: "btn btn-danger" %></td>
  </tr>
<% end %>